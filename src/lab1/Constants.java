package lab1;

/**
 * Contains Golden ratio and max number in luke sequence constants
 *
 * @author Ruslan
 * @version 1.0.0
 */
public class Constants {
    static int MAX_LUKE_NUMBER = 89;
    static double GOLDEN_RATIO = (1 + Math.sqrt(5)) / 2.0;
}
